import { ComingSoonPage } from "./ComingSoonPage";

const Pricing = () => (
  <ComingSoonPage
    code="SECTION 06 / PRICING"
    heading={<>Pick your<br /><span className="text-hazard">production lane.</span></>}
    subtitle="Pay for the workflow that replaces scratchpads: defaults, recipes, batch math, oil tracking, COGS, Square sync, Shopify sync, and catalog-backed swap matching."
    watermark="PRICING"
    cta="OPEN HQ"
  />
);
export default Pricing;
